services:
  backend:
    build:
      context: ./backend
    container_name: mejuvante-backend
    ports:
      - '8080:8080'

  frontend:
    build:
      context: ./frontend
    container_name: mejuvante-frontend
    depends_on:
      - backend
    ports:
      - '80:80'

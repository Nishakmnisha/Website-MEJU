name: Verify Repository Files

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  verify-files:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify required files exist
        run: bash scripts/verify_repo_files.sh
